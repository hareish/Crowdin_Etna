{% extends 'base.html.twig' %}

{% block title %}Lang{% endblock %}

{% block body %}
    <h1>Lang</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lang.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ lang.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lang_index') }}">back to list</a>

    <a href="{{ path('app_lang_edit', {'id': lang.id}) }}">edit</a>

    {{ include('lang/_delete_form.html.twig') }}
{% endblock %}
